# ── 依赖 ──────────────────────────────────────────
node_modules/

# ── 构建产物 ──────────────────────────────────────
frontend/dist/
frontend/.vite/
backend/dist/

# ── 数据（用户数据/录音/PDF/数据库，绝不入 git） ─
data/
logs/

# ── 证书与密钥 ────────────────────────────────────
frontend/certs/
*.pem
*.key
.env
.env.*
backend/.env
frontend/.env

# ── 系统/编辑器 ───────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# ── iOS 原型项目（保留但不再开发） ────────────────
Morning Reader.xcodeproj/xcuserdata/
*.xcuserdatad/

# ── Claude / 临时 ─────────────────────────────────
.claude/
*.log
*.tmp

# Sprint 0A 备份目录与本地配置（环境敏感）
data.backup-*/
**/nas.env
**/cloudflared.env

# 设计 mockup（Homer 决定后再入库）
design-mockups/
